[pytest]
addopts = -p no:cacheprovider
testpaths = tests
markers =
    unit: Fast tests that run without live external services.
    integration: Tests that require live external services or heavyweight environment setup and should stay out of the default PR CI run.
